// Removed — was a single hardcoded fake photo post (not backed by any real
// feed data; DockIn has no public-feed concept, only 1:1 ephemeral snaps).
// Kept as an empty file rather than deleted because the sandbox couldn't
// unlink it; safe to actually delete this file by hand.
export {};
