// Removed — replaced by AvatarStatsHero.tsx. Was showing a hardcoded fake
// streak value ("42"). Kept as an empty file rather than deleted because
// the sandbox couldn't unlink it; safe to actually delete this file by hand.
export {};
