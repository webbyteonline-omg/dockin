// Removed — was hardcoded fake "Kabir is at Mess 2" / "Riya is at Central
// Library" status lines, not backed by any real location data. Kept as an
// empty file rather than deleted because the sandbox couldn't unlink it;
// safe to actually delete this file by hand.
export {};
