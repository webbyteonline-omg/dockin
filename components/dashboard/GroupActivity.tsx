// Removed — was a hardcoded fake "Dance Society — Thumka" poll card, not
// backed by any real group/poll data. Kept as an empty file rather than
// deleted because the sandbox couldn't unlink it; safe to actually delete
// this file by hand.
export {};
