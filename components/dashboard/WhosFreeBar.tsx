// Removed — was a hardcoded fake "3 friends are free after 4 — Riya, Kabir,
// Priya" card, not backed by any real availability/presence data. Kept as
// an empty file rather than deleted because the sandbox couldn't unlink it;
// safe to actually delete this file by hand.
export {};
